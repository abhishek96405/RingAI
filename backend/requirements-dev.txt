# Duuutah AI — Test-only dependencies.
# This file is read by the test suite and CI. It is intentionally separate
# from backend/requirements.txt so that production deps remain untouched.
#
# Install: pip install -r backend/requirements-dev.txt
#
# pytest, httpx, and black are already pinned in backend/requirements.txt;
# the versions below are kept consistent.

pytest==9.0.2
# pytest-asyncio 1.3.0 is the first release that supports pytest 9
# (pytest<10,>=8.2). Earlier 1.x and 0.24 are pinned to pytest<9.
pytest-asyncio==1.3.0
pytest-cov==6.0.0
pytest-mock==3.14.0
pytest-xdist==3.6.1
pytest-env==1.1.5
pytest-timeout==2.3.1
pytest-randomly==3.16.0
respx==0.21.1
mongomock-motor==0.0.34
freezegun==1.5.1
faker==30.8.1
hypothesis==6.118.7
# syrupy 4.x is pinned to pytest<9; 5.x supports pytest 9.
syrupy==5.2.0
httpx==0.28.1
